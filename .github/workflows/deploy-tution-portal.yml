name: Deploy Tution Portal to Cloudflare Pages

on:
  push:
    branches:
      - master
    paths:
      - 'tution-portal/**'
      - 'packages/**'
      - '.github/workflows/deploy-tution-portal.yml'
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'
          cache-dependency-path: 'tution-portal/package-lock.json'

      # tution-portal depends on @chhsban/kv-utils via a "file:" dependency; its dist/
      # is gitignored, so it must be built here before tution-portal's npm ci copies it in.
      # `|| true`: src/teacher-verify.ts has a pre-existing, unrelated KVNamespace type
      # error (missing @cloudflare/workers-types ambient types) that makes tsc exit
      # non-zero; tsc still emits dist/ output regardless (noEmitOnError is not set).
      - name: Build @chhsban/kv-utils
        working-directory: packages/kv-utils
        run: |
          npm install
          npm run build || true

      - name: Install dependencies (tution-portal)
        working-directory: tution-portal
        run: npm ci

      - name: Build tution-portal
        working-directory: tution-portal
        run: npm run build
        env:
          VITE_API_BASE_URL: https://tution-system.workers.dev/api

      - name: Deploy to Cloudflare Pages
        working-directory: tution-portal
        env:
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
        run: |
          npm install -g wrangler
          wrangler pages deploy dist --project-name=chhsban-tution --commit-dirty=true
            CLOUDFLARE_API_TOKEN

      - name: Comment on commit
        if: always()
        uses: actions/github-script@v7
        with:
          script: |
            const status = '${{ job.status }}' === 'success' ? '✅ Deploy succeeded' : '❌ Deploy failed';
            console.log(`${status}: chhsban-tution.pages.dev`);
