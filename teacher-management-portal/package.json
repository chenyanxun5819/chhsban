{
  "name": "teacher-management-portal",
  "version": "1.0.0",
  "description": "教師資料管理系統 - Cloudflare Pages 管理前端",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "clean": "rmdir /s /q dist 2>nul || true",
    "deploy": "npm run clean && npm run build && wrangler pages deploy dist --commit-dirty=true"
  },
  "dependencies": {
    "axios": "^1.6.0"
  },
  "devDependencies": {
    "vite": "^5.0.0",
    "typescript": "^5.3.0",
    "@types/node": "^20.0.0"
  }
}
